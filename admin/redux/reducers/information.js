const information = cwp_global.informations;

const initialState = {
	cards: information.cards,
};

export default function (state = initialState, action) {
	switch (action) {
		default:
			return state;
	}
}
